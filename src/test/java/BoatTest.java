import org.junit.Test;

import static org.junit.Assert.*;

public class BoatTest {

//should create boat with size, and initial coordinate.
// boat size should be bigger than 1
// boat size should be smaller than 6
// should generate other coordinates based on initial coordinate and size.
// should validate coordinates to be within board limits (a9, a10, a11 is invalid)
}